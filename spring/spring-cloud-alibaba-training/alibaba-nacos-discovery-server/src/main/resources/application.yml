spring:
  profiles:
    active: local
  application:
    name: alibaba-nacos-discovery-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: local
        register-enabled: true
server:
  port: 8081

